* [v] vim `:h`

    ```vim
    :set autochdir   " 自动将当前目录切换为当前打开文件所在的目录
    ```

* [v] 处理 1 条 log

* [v] 任务计划要点提交吗？

* [v] 编译 siccl graph 部分

* [v] 处理 1 条 log

* [v] 编译并测试 siccl graph 的 case

* [v] 处理 1 条 log

* [v] `vim `b#`

    :b# 会切换到“交替文件”（alternate file），即当前窗口上一次打开的那个文件。

* [v] 处理 1 条 log

* [v] 从 xml.h/.cc 文件开始梳理

* [v] 处理 1 条 log

* [v] `:bd | b#`

    删除当前 buffer，并立即切换到交替文件

    在 split 分屏中执行这个，和先执行`:bd`，再执行`:b#`有什么区别？前者是否能保证 split 分屏不被关闭？

* [v] 处理 1 条 log

* [v] 重新整理 xml 要处理的任务，通过编译

* [v] 处理 1 条 log

* [ ] python str glob

* [ ] csv.reader()

* [ ] rng = np.random.default_rng(), rng.shuffle()

* [ ] grep -q

* [ ] cut -d' ' -f1

* [ ] `dracut -f`

* [ ] vim 自定义命令

    ```vim
    " 定义命令
    command! -range FindSharp <line1>,<line2>call FindNextSharp()

    " 在 Visual 模式下使用：
    :'<,'>FindSharp
    ```

* [ ] vim ctrl + r

    在命令行中按 <C-r>= 可以插入表达式结果：

    ```vim
    " 在 Visual 模式下按 :，然后：
    :normal gv<C-r>=FindTargetLine()<CR>G
    ```

* [ ] egrep

    ```bash
    # 检查 CPU 虚拟化支持
    egrep -c '(vmx|svm)' /proc/cpuinfo

    # 检查 IOMMU 支持
    sudo dmesg | grep -E 'DMAR|IOMMU'
    ```
* [ ] git cherry-pick

* [ ] sudo sh -c "echo 1 > /sys/class/iommu/dmar*/reset"

* [ ] iommu=relaxable iommu.forcedac=0 iommu.strict=0

* [ ] `echo "vfio-pci" > /sys/bus/pci/devices/0000:01:00.0/driver_override`

* [ ] `ls -l /sys/bus/pci/devices/0000:01:00.0/iommu_group/devices/`

* [ ] 

    ```bash
    sudo usermod -L admin
    sudo passwd -l admin
    ```

    或者直接删除：

    ```bash
    sudo userdel -r admin
    ```

    ```bash
    ip neigh | grep 100.104.94.244
    ```

    ```bash
    arp -an | grep 100.104.94.244
    ```

* [ ] 什么是 mintty, ConEmu

    `.screenrc` 中配置 `term screen` 是什么意思

    `TERM=xterm-256color`是什么意思

* [ ] cywgin 中是否默认安装了`ncurses`和`terminfo`
