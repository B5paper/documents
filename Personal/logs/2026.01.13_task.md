* [ ] pci busid 改成 uuid

* [v] 完成 02_get_topo.cpp

* [ ] 调研下述说法是否正确

    ❌ Gemini 的说法

    .gitmodules 里的 url = ../repo-server-2 是相对于主仓库的远程地址（origin URL）

    这句话 只有在“主仓库是远程 URL（如 https://、ssh://）”时才成立，
    在你这个 case（本地路径 clone）里是错误的理解延伸。

* [ ] `git submodule sync`

* [v] matlab `a = [x, y, ]`

* [v] nv 环球中拿到包含 rdma 的 xml

* [v] 将 rdma xml 修改成 sipu 兼容的格式，制作 mock xml

* [ ] 运行 01 load topo test case, 看是否能正常加载 mock xml

* [new] 调研`pci nodes: num: 10`的问题，nv 环境下是 8

    deps:

    * [ ] 先排查是否是 cude visible devices 的问题

        * 使用新的 xml 再跑一遍 test 01

    * [ ] 修改 nv 中的代码，输出 topo system 内容

        * 主要是 nodes 和 links

        * pci 节点需要输出 bdf

            可能需要从 topo id 中提取出来

* [new] 调研 cpu link 只有 3 条的问题，nv 对应为 5 条，4 pci, 1 cpu

* [new] 调研 nic nodes: num: 4 的问题，nv 对应为 6 个 nic 节点

    net 同理，也需要调研。