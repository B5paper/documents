[processed]

* [v] informal branch 是否能跑通？

    如果能，那么 stash 文件属于 c2c branch

    先在 c2c 提交，再与 host-dev 做比较后删除多余文件。

* [v] c2c 是否能跑通？

    若能，那么和 host-dev 只差一个 commit，只需要 reset 后挑选修改即可。

    若不能，说明 informal 中的改动未完全同步到 c2c，需要 git merge informal

* [v] host-dev + 最新 sdk 是否能跑通？

    若不能，那么看是否仍有 0x128 报错，若有，则修完 stream + comm operator 问题后看是否能跑通。
