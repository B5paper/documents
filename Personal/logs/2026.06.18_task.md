* [v] .env 是只影响 prompt，还是也会影响 f5 debug?

    10:35 ~ 10:47: 都会影响

* [ ]     方法五：使用 pyproject.toml（现代Python项目推荐）

    创建 `pyproject.toml` 文件：
    ```toml
    [build-system]
    requires = ["setuptools", "wheel"]

    [tool.setuptools.packages.find]
    where = ["."]
    include = ["*"]
    ```

* [ ] os.getcwd()

    os.environ.get()

    os.path.abspath()

    os.path.join()

    sys.path

    os.path.dirname()

    os.path.exists()

    sys.path.insert()

* [ ] os.chdir()

* [ ] set -a, set +a

* [ ] python-dotenv

* [ ] git cherry-pick

* [ ] cygpath

* [ ] git annes