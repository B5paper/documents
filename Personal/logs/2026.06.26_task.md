* [v] 处理 1 个 log

* [v] omni vim

    ```vim
    filetype plugin on
    set omnifunc=syntaxcomplete#Complete
    ```

* [v] 处理 1 个 log

* [v] os.getcwd()

    os.environ.get()

* [v] 处理 1 个 log

* [v] 调研 set ff=unix 的作用，因为它似乎确实无法直接删除 ^M

* [v] 处理 1 个 log

* [ ] review vim `autocmd` 的用法，

    解释`autocmd FileType python set omnifunc=python3complete#Complete`

* [ ] 处理 1 个 log

* [ ] vim omni

    ```vim
    " 自动触发补全
    set completeopt=menuone,noinsert,noselect
    set shortmess+=c  " 避免补全菜单显示消息

    " 映射快捷键
    inoremap <expr> <Tab> pumvisible() ? "\<C-n>" : "\<Tab>"
    inoremap <expr> <S-Tab> pumvisible() ? "\<C-p>" : "\<S-Tab>"
    inoremap <expr> <CR> pumvisible() ? "\<C-y>" : "\<CR>"
    ```
