[processed]

* [new] matlab `length()`

* [new] matlab `a = {x, y, z};`

* [new] matlab `a = [x, y, ]`

* [new] matlab `cellfun`

    ```matlab
    % 或者使用 cellfun
    file_cell = {'file1.txt', 'file2.csv'};
    names = cellfun(@(x) fileparts(x), file_cell, 'UniformOutput', false);
    ```

* [new] matlab `which()`