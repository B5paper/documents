* 讲讲 vim 中 * 和 {-} 的匹配含义，Match the preceding (meta)character 0 or more time.

    在 Vim 的正则表达式中，你提到的 `*` 和 `{-}` 确实都表示“匹配前面的字符 0 次或多次”。

    但它们之间有一个至关重要的区别：**`*` 是“贪婪的”（Greedy），而 `{-}` 是“非贪婪的”（Non-greedy / Lazy）**。一个是“大胃王”，能吃多少吃多少；另一个是“点到为止”，够用就行。

    1. `*` —— 贪婪匹配（尽可能多）

        `*` 会吞掉尽可能多的字符，直到不满足匹配条件为止。它会一路向后狂奔，直到整行的最后一个匹配点。

        * **语法**：在 Vim 中直接写 `*` 即可。
        * **特点**：从左边开始匹配，一直匹配到**最右边**符合条件的终点。

    2. `{-}` —— 非贪婪匹配（尽可能少）

        `{-}` 相当于其他现代正则引擎（如 Python、JavaScript、PCRE）中的 `*?`。它非常懒惰，只要一达到匹配的最低要求（哪怕只有 0 次或 1 次），它就会立刻停下来。

        * 语法：

            * 在 Vim 的**默认模式**下，你需要加反斜杠：`\{-}`
            * 如果在 **Very Magic 模式**（即加了 `\v`）下，可以像你写的那样直接写 `{-}`

        * 特点：从左边开始匹配，只要遇到**第一个**符合条件的终点就立刻收工。

    **实战对比：一眼看懂区别**

    假设我们有下面这行 HTML 文本：

    ```html
    <h6>坚持学习</h6><h6>天天向上</h6>
    ```

    我们想要匹配 `<h6>...</h6>` 标签及其内部的内容。

    **表现 1：使用贪婪匹配 `*`**

    * **模式**：`<h6>.*</h6>`
    * **匹配结果**：
    `<strong><h6>坚持学习</h6><h6>天天向上</h6></strong>`
    * **为什么？**
    `.*` 看到整行最后还有一个 `</h6>`，于是它直接把中间的 `</h6><h6>` 也当成普通字符给“吞”掉了，直接一路匹配到了大后期。

    **表现 2：使用非贪婪匹配 `{-}`**

    * **模式**：`<h6>.\{-}</h6>` （或 `\v<h6>.{-}</h6>`）
    * **匹配结果**（会匹配到两个独立的结果）：
    1. `<strong><h6>坚持学习</h6></strong>`
    2. `<strong><h6>天天向上</h6></strong>`

    * **为什么？**

    `.\{-}` 只要一看到紧接着出现了 `</h6>`，就立刻停止当前的匹配。所以它能精准地把两个标签分别揪出来。

    **总结与避坑指南**

    | 特性 | `*` | `{-}` |
    | --- | --- | --- |
    | **匹配次数** | 0 次或多次 | 0 次或多次 |
    | **匹配策略** | 贪婪（尽可能多） | 非贪婪（尽可能少） |
    | **现代正则对应** | `*` | `*?` |
    | **Vim 默认写法** | `*` | `\{-}` (别漏了反斜杠) |

    > **Vim 小贴士**： 在 Vim 中用非贪婪匹配时，很多人容易漏掉反斜杠写成 `{-}`，导致 Vim 把它当成了普通字符串处理。如果不喜欢写反斜杠，记得在正则开头加上 `\v`（Very Magic 模式），例如：`/\v.{-}`。

    vim 中贪婪和非贪婪匹配的对照表：

    | Greedy quantifier | Non-greedy quantifier | Description |
    | - | - | - |
    | `*` | `{-}` | Match the preceding (meta)character 0 or more time. |
    | `+` | `{-1,}` | Match the preceding (meta)character 1 or more time. |
    | `=` | `{-0,1}` | Match the preceding (meta)character 0 or 1 time. |
    | `{n,m}` | `{-n,m}` | Match the preceding (meta)character from n to m times. |
    | `{n}` | `{-n}` | Match the preceding (meta)character exactly n times. |
    | `{,m}` | `{-,m}` | Match the preceding (meta)character from 0 to m. |

* cached tabs

    * qemu edu设备介绍

        <https://zhuanlan.zhihu.com/p/350947593>

    * EDU device

        <https://www.qemu.org/docs/master/specs/edu.html>

    * NLP From Scratch

        <https://docs.pytorch.org/tutorials/intermediate/char_rnn_classification_tutorial.html>

* PyTorch Functional Transforms for Computer Vision

    ```py
    import torchvision.transforms.functional as F
    import torch
    from PIL import Image

    image = Image.open('nature.jpg')

    # brightness
    # brightness_factor = 0, black output image.
    # brightness_factor = 1, the original image.
    # 0 < brightness_factor < 1, a darker output image.
    # brightness_factor > 1, a brighter output image.
    output = F.adjust_brightness(image, brightness_factor=3.0)
    output.show()

    # contrast
    # Solid gray image if contrast_factor = 0.
    # The original image if contrast_factor = 1,
    # To lower contrast use 0 < contrast_factor < 1.
    # To increase contrast use contrast_factor > 1.
    output = F.adjust_contrast(image,contrast_factor=4.2)

    # hue
    # To adjust the hue, the image is first converted to HSV (Hue, Saturation, Value) color space. The image hue is adjusted by converting the image to HSV and the changes are made in the hue channel (H). Then the image is converted back to the original image. 
    # The important parameter to this transform is hue_factor.  It's range value is [-0.5, 0.5].  0.5 and -0.5 give the complete opposite of the hue channel in HSV color space in positive and negative direction respectively. Therefore, both hue_factor=-0.5 and hue_factor=0.5 gives an image with complementary colors while hue_factor=0 gives the original image.
    output = F.adjust_hue(image,hue_factor=-0.3)

    # saturation
    output = F.adjust_saturation(image,saturation_factor=7.0)

    # sharpness
    output = F.adjust_sharpness(image,sharpness_factor=5.0)

    # gaussian_blur
    output = F.gaussian_blur(image, kernel_size = (11,13), sigma = None)

    # pil_to_tensor
    img_tensor = F.pil_to_tensor(pil_image)

    # to_pil_image
    pil_img = F.to_pil_image(img_tesnor)

    # normalize
    img_tensor = F.pil_to_tensor(image)
    img_tensor = img_tensor.type(torch.float)
    mean, std = img_tensor.mean(), img_tensor.std()
    img = F.normalize(img_tensor, mean=mean, std=std)
    output = F.to_pil_image(img)
    output.show()

    # invert
    output = F.invert(image)
    ```

* vim 是否可以在启动时加载指定的 .vimrc 文件？或者启动后加载指定的 vimscript？我现在想开发 vim 插件，想有个可以快速验证效果的调试环境

    完全没问题。Vim 提供了非常灵活的参数来指定启动配置文件，这对于开发插件或测试特定配置（而不干扰你日常使用的 `.vimrc`）非常有帮助。

    以下是实现你需求的几种主要方式：

    ### 1. 启动时加载指定的 `.vimrc`

    如果你想让 Vim 忽略默认的配置文件，转而加载一个你正在测试的文件，可以使用 `-u` 参数：

    ```bash
    vim -u /path/to/your/test_vimrc

    ```

    * **注意**：使用 `-u` 会跳过所有默认的初始化脚本（包括插件加载）。如果你希望在加载自定义配置的同时保持环境尽量“纯净”，这是最佳选择。
    * **加载 NONE**：`vim -u NONE` 可以让你在一个完全不加载任何配置的环境下启动，适合排查是否是某个插件导致的 Bug。

    ---

    ### 2. 启动后加载指定的 Vimscript

    如果你只是想在 Vim 启动并进入界面后立即执行一段脚本（比如初始化你的插件代码），可以使用 `-S` 参数（Source）：

    ```bash
    vim -S /path/to/your/plugin_script.vim

    ```

    这相当于在 Vim 启动后手动执行了 `:source /path/to/your/plugin_script.vim`。

    ---

    ### 3. 开发插件时的“快速验证”工作流

    为了提高开发效率，你可以组合使用上述技巧，甚至配合 **Vim 的双窗格** 或 **实时重载**：

    #### A. 命令行组合拳

    如果你正在开发一个插件文件夹，可以这样启动：

    ```bash
    # --cmd 会在加载 .vimrc 之前执行命令
    # -S 会在加载 .vimrc 之后执行命令
    vim --cmd "set rtp+=./my_plugin_folder" -S tests/test_case.vim

    ```

    #### B. 在 Vim 内部实时重载

    在开发时，最快的方法是给“重新加载当前文件”设置一个快捷键。在你的测试配置中加入：

    ```vim
    " 按下 \r 重新加载当前正在编辑的脚本
    nnoremap <leader>r :source %<CR>:echo "Script reloaded!"<CR>

    ```

    #### C. 使用 -i NONE 避免污染

    在调试时，你可能不希望 Vim 记录这次会话的历史记录、Marks 或寄存器。可以加上 `-i NONE`：

    ```bash
    vim -u test_vimrc -i NONE

    ```

    ---

    ### 总结建议

    如果你想搭建一个**隔离的调试环境**，建议创建一个目录结构如下：

    * `debug/`
    * `min_vimrc` (只包含最基础的配置和插件路径)
    * `test_script.vim` (你要测试的代码)



    然后使用以下命令启动：

    ```bash
    vim -u debug/min_vimrc -S debug/test_script.vim

    ```

    **需要我帮你写一个基础的 `min_vimrc` 模板，用来自动加载你正在开发的插件路径吗？**

* execute lnum . 'normal! :s/<Tab>/ | /g'
    分析一下这条 vimscript 是否有问题

    这条指令在语法逻辑上是**存在问题的**，直接运行通常会报错。主要的症结在于你混淆了 **Vimscript 表达式执行**（`execute`）和 **Vim 普通模式指令**（`normal!`）的边界。

    以下是详细分析及修正建议：

    ---

    ### 1. 核心问题分析

    #### A. `normal!` 后面不能直接跟命令行模式命令

    `normal!` 的作用是模拟你在键盘上按下按键。`:s/...` 是一个命令行模式（Command-line mode）命令。
    如果你想在 `normal!` 里执行冒号命令，你必须显式地写入回车键（`<CR>`），否则 Vim 只是在底行输入了字符而没有回车。

    * **错误逻辑：** `normal! :s/...` (Vim 会在底行输入文字然后停在那里)
    * **修正逻辑：** `normal! :s/...^M` (其中 `^M` 是回车)

    #### B. 变量 `lnum` 的连接方式

    在 `execute` 中，如果你想在特定行执行替换，通常不需要通过 `normal!` 中转。`execute` 本身就可以拼接出完美的命令行指令。

    ---

    ### 2. 修正方案

    根据你的意图（在 `lnum` 行将 `Tab` 替换为 `|`），这里有三种更好的写法：

    #### 方案一：最推荐（直接使用 execute）

    这是最干净、最高效的写法，直接构造 `:行号s/源/宿/` 命令。

    ```vim
    execute lnum . 's/\t/ | /g'

    ```

    * **优点**：不需要进入 `normal` 模式，直接作用于目标行。
    * **注意**：在字符串里表示 Tab，用 `\t` 比直接写 `<Tab>` 更可靠。

    #### 方案二：如果你坚持要用 normal!

    如果你是为了练习 `normal!` 的用法，必须加上 `\n` 或 `<CR>`，并处理好转义：

    ```vim
    execute lnum . "normal! :s/\\t/ | /g\<CR>"

    ```

    * **缺点**：非常繁琐，需要处理双重转义（`\\t`）。

    #### 方案三：使用函数式写法（现代 Vim/NeoVim）

    如果你在写插件，使用 `setline()` 和 `substitute()` 会更安全，不会污染搜索历史：

    ```vim
    let line_content = getline(lnum)
    let new_content = substitute(line_content, '\t', ' | ', 'g')
    call setline(lnum, new_content)

    ```

    ---

    ### 3. 避坑指南：关于 `<Tab>`

    在 Vimscript 字符串中，直接写 `"<Tab>"` 往往不会被识别为制表符。

    * 如果你想匹配 **Tab 键**：请使用 `\t`。
    * 如果你想匹配 **已经存在的字符串 "<Tab>"**：请使用 `\<Tab>`。

    ---

    ### 总结

    你的原指令修改为：
    `execute lnum . 's/\t/ | /g'` 即可完美运行。

    **你在开发插件时，是否需要处理“如果行内没有 Tab 会报错”的情况？我可以帮你加个 `e` 标志位（忽略错误）。**

* 在贝叶斯网络中，很多条训练数据最后化为了 count 与频率，我认为贝叶斯网络提取到的信息非常非常有限。帮我完善整个思路。有没有什么改进的方法？

    你的直觉非常敏锐。在基础的贝叶斯网络（Bayesian Networks, BNs）学习中，如果仅仅通过极大似然估计（MLE）将数据转化为计数和频率，确实会忽略大量的信息，甚至导致模型极其脆弱。

    这种“信息有限”的感觉主要源于**离散化带来的信息损失**、**样本稀疏性**以及**对因果机制的过度简化**。

    为了帮你完善思路，我们可以从以下几个维度来深入探讨贝叶斯网络的局限性及其改进方案：

    ---

    ## 1. 为什么“计数”让你觉得信息有限？

    在传统的表格型条件概率表（CPD）中，我们确实只是在做“填表”工作。这种做法存在三个核心痛点：

    * **维度灾难：** 如果一个节点有 5 个父节点，每个节点有 3 个状态，表格大小就是 $3^5=243$。对于很多组合，数据中可能根本没有观测值（计数为 0），导致模型无法泛化。
    * **忽略了顺序和程度：** 简单的计数把“稍微超过阈值”和“远超阈值”的数据等同对待（如果进行了硬离散化）。
    * **静态假设：** 计数通常不考虑数据随时间演化的趋势。

    ---

    ## 2. 改进方法：如何提取更深层的信息？

    要让贝叶斯网络变得“聪明”起来，不再仅仅是简单的加法器，可以尝试以下几个方向：

    ### A. 从“频率派”转向“贝叶斯派” (引入先验)

    不要只用数据说话，要用“经验 + 数据”说话。

    * **贝叶斯参数估计：** 使用 **Dirichlet 分布** 作为先验。即使某个事件计数为 0，通过先验分布（伪计数），模型依然能给出一个合理的概率估计。
    * **优势：** 解决了数据稀疏问题，使模型在小样本下更稳健。

    ### B. 使用参数化分布代替表格 (Local Structure)

    不要用死板的表格，而是用函数来描述变量间的关系。

    * **Noisy-OR / Noisy-Max 模型：** 假设多个因果因素是独立起作用的。这能将参数量从指数级降低到线性级。
    * **逻辑回归/神经网络 CPD：** 节点的条件概率不再靠查表，而是通过一个逻辑回归模型或小型神经网络计算得出。
    > ---
    > 
    > 


    * **优势：** 能够捕捉变量间复杂的非线性互动，而不是机械地统计组合频率。

    ### C. 处理连续变量 (混合贝叶斯网络)

    不要强行把所有数据都变成“红、绿、灯”或“高、中、低”。

    * **高斯贝叶斯网络 (GBN)：** 假设节点服从线性高斯分布。
    * **非参数化方法：** 使用核密度估计（KDE）来学习变量间的关系，完全摆脱对特定分布形式的依赖。

    ### D. 结构学习的改进 (不仅是学参数，还要学逻辑)

    如果你觉得信息提取有限，可能是因为网络结构本身没搭好。

    * **因果发现 (Causal Discovery)：** 使用 PC 算法或带有得分函数（如 BIC, AIC）的搜索算法，自动发现变量间的隐性因果链条，而不仅仅是相关性。
    * **隐变量模型 (Latent Variables)：** 引入数据中未直接观测到的“隐节点”（类似深度学习中的隐藏层）。这能解释观测数据中无法解释的共变关系。

    ---

    ## 3. 进阶方案：贝叶斯与深度学习的融合

    如果你希望像深度学习那样提取极高维的信息，可以关注以下前沿方向：

    | 方法 | 核心思路 |
    | --- | --- |
    | **变分自编码器 (VAE)** | 本质上就是一个带有特定结构的深层贝叶斯网络，能够提取复杂的特征表示。 |
    | **贝叶斯神经网络 (BNN)** | 给神经网络的每一个权重都加一个概率分布，既有深度的表达能力，又有概率的确定性衡量。 |
    | **图神经网络 (GNN)** | 在图结构上进行信息传递。贝叶斯网络可以看作是 GNN 的一种特例，利用消息传递机制（Message Passing）提取拓扑信息。 |

    ---

    ## 4. 总结与建议

    你认为“信息有限”是因为**传统的表格型参数学习把多维的、连续的、具有因果逻辑的世界强行压缩成了频率表**。

    **完善思路的下一步：**

    1. **突破“离散”：** 尝试研究连续变量的处理方法。
    2. **引入“先验”：** 思考领域知识如何通过 Dirichlet 分布注入模型。
    3. **函数化 CPD：** 尝试用简单的线性模型或逻辑回归代替概率表。

    你目前是在处理具体的业务问题（如故障诊断、风险评估），还是在进行纯理论的研究？了解你的**应用场景**，我可以为你推荐更具体的算法包或实现工具。

* struct A
    {
        void func_1()
        {
            return 0;
        }
    }
    vim 中，visual 模式下选中 void func_1() 后，我希望快速跳转到 v 和 ) 怎么办？

    在 Vim 的 Visual 模式下，当你已经选中了 `void func_1()`，最快的方式是利用 **`o`** 键。

    ### 核心操作：`o` (Go to Other end)

    在 Visual 模式（或 Visual Line 模式）下，按下 **`o`** 会将光标切换到选中区域的**另一端**。

    1. **跳转到 `v**`：如果光标当前在 `)`，按下 `o`，光标会立刻跳回起始点 `v`。
    2. **跳转到 `)**`：如果光标当前在 `v`，按下 `o`，光标会跳到结束点 `)`。

    ---

    ### 其他进阶技巧

    如果你想在选中状态下进行更细微的调整，可以使用以下方法：

    * **精确移动**：
    * 按下 `o` 切换到你想调整的那一端。
    * 使用 `f` 或 `t` 进行行内快速定位。例如，按下 `o` 回到开头后，再按 `f(` 就能精准定位到左括号。


    * **重新选择（更符合 Vim 思路的方式）**：
    如果你经常需要选中函数名或括号内容，可以尝试使用 **Text Objects**，这比手动涂抹选中更高效：
    * `viw`：选中光标下的单词（如 `func_1`）。
    * `vi(`：选中括号内的内容。
    * `va(`：选中括号及其内部内容。



    ---

    **你想让我为你整理一份关于 Vim 快速跳转和文本对象（Text Objects）的常用快捷键清单吗？**

* 那`[和`]呢？

    在 Vim 中，**`[`** 和 **`]`**（特别是配合反引号或撇号使用的 ``[` 和 ``]`）是非常强大的工具，它们专门用于定位**上一次操作（修改或拉取）的边界**。

    在 Visual 模式下，它们的具体作用如下：

    ### 1. 精准跳转到选区的起点和终点

    如果你已经在 Visual 模式下选中了一段内容，这两个组合键可以让你瞬间到达边界：

    * **``[`**：跳转到上一次选区（或修改）的**起始位置**（即 `v`）。
    * **``]`**：跳转到上一次选区（或修改）的**结束位置**（即 `)`）。

    > **注意：** 使用反引号 ``` 会定位到具体的行列，而使用单引号 `'[` 则只会定位到那一行的行首。

    ---

    ### 2. 与 `o` 的区别

    虽然它们都能在选区两端跳转，但逻辑逻辑略有不同：

    | 快捷键 | 行为特征 |
    | --- | --- |
    | **`o`** | **切换端点**。如果光标在头，按 `o` 去尾；如果光标在尾，按 `o` 去头。非常适合临时调整选区范围。 |
    | **``[` / ``]**` | **绝对定位**。无论光标当前在哪，``[` 永远回起点，``]` 永远去终点。 |

    ---

    ### 3. 实际应用场景（不仅限于 Visual 模式）

    这两个标记在普通模式下极其有用，因为 Vim 会自动在以下操作后更新它们：

    * **粘贴（Put）后**：如果你刚按 `p` 粘贴了一大段代码，按 **``]`** 可以立刻跳到粘贴内容的末尾，方便你继续编写。
    * **修改（Change）后**：刚用 `cw` 或 `c/pattern` 改完代码，想要跳回改动的地方，可以用这两个标记。
    * **重新选中**：如果你退出了 Visual 模式，想再次选中刚才那段代码，可以输入 **`gv`**，它的底层逻辑其实就是引用了这两个边界标记。

    ---

    ### 总结

    对于你提到的 `void func_1()`：

    1. 按 **`v`** 进入选中。
    2. 移动到 **`)`** 结束。
    3. 此时想回 **`v`**：按 **`o`** 或者 **``[`**。
    4. 此时想回 **`)`**：按 **`o`** 或者 **``]`**。

    **需要我演示一下如何利用这些标记配合 `operator`（如删除或修改）来实现更复杂的自动化编辑吗？**

* 但是`[和`]定位的是“上一次的”，不是当前 visual 模式下选中的。你再想想

    你观察得非常敏锐，确实是我之前的描述不够精确。

    你说得对：**在 Vim 正在进行的 Visual 模式中，`[` 和 `]`（及其对应的标记 ``[` 和 ``]`）定位的是“上一次”操作的边界，而不是“当前正在变动中”的选区边界。**

    为了说清楚这个问题，我们需要拆解一下 Vim 的底层逻辑：

    ### 1. 正在选中时：`o` 是唯一的“跳转键”

    当你输入 `v` 选中 `void func_1()` 时，Vim 内部维护了两个点：

    * **锚点 (Anchor)**：你开始按 `v` 的地方（`v`）。
    * **光标点 (Cursor)**：你当前移动到的地方（`)`）。

    在 **Visual 模式活跃期间**，除了通过 `o` (other end) 在锚点和光标点之间切换，并没有其他内置的快捷键能直接跳到“当前选区的开头”。

    ### 2. `[` 和 `]` 到底什么时候更新？

    正如你所指出的，它们是“延迟更新”的。

    * 当你**结束** Visual 模式（比如按 `y` 复制了，或者按 `Esc` 退出了），Vim 才会把刚刚这段区域的起始和终点赋值给 ``[` 和 ``]`。
    * 所以，如果你正在选中，按 ``[` 实际上会跳到你**上一次**复制或修改的代码块起点，这会导致你直接脱离当前的 Visual 模式，飞到别处去。

    ### 3. 一个容易混淆的标记：`<` 和 `>`

    既然提到了边界，不得不提 ``<` 和 ``>`：

    * 它们专门指向**上一次选区**的开头和结尾。
    * 同样，它们也只在 Visual 模式**结束后**才更新。
    * **gv** 命令之所以能重选上次区域，本质上就是执行了 `v`<`> `。

    ---

    ### 总结：在 Visual 模式下操作的“真相”

    如果你已经选中了 `void func_1()`，光标在 `)`：

    | 目标 | 操作 | 备注 |
    | --- | --- | --- |
    | **跳转到 `v` 并继续选区** | 按 **`o`** | 这是最快、且唯一保持选区状态的办法。 |
    | **跳转到 `v` 但退出选区** | 按 **`Esc`** 然后 **`o`** (非标准) | 通常在 Vim 里不这么做。 |
    | **跳到上一次操作处** | 按 **``[`** | 会直接中断当前的 Visual 模式。 |

    **补充一个极端技巧：**
    如果你是因为选区太长（比如选了好几屏），想看一眼开头再回来，你会发现 `o` 非常好用。按一下 `o` 回到开头看一眼，再按一下 `o` 回到结尾继续往下选。

    **既然你对标记（Marks）的更新时机这么感兴趣，需要我为你对比一下 ``[`（操作边界）与 ``<`（选区边界）在实际工作流中的具体差异吗？**

* 好

    既然你对 Vim 的内部逻辑这么细致，那我们直接拆解这两对最容易混淆的“边界标记”。它们就像 Vim 的**自动快照**，但在不同的时刻按下快门。

    ### `[` / `]`  vs  `<` / `>` ：深度对比

    | 标记对 | 名称 | **什么时候“按下快门”？** (更新时机) | 主要用途 |
    | --- | --- | --- | --- |
    | **``[` 和 ``]**` | **操作边界** | 当你执行了 **Operator**（如 `y` 复制, `d` 删除, `p` 粘贴, `c` 修改）之后。 | 跳到刚粘贴内容的末尾，或刚改动代码的起点。 |
    | **``<` 和 ``>**` | **选区边界** | 当你**退出** Visual 模式的那一瞬间。 | 重新选中（`gv`）、针对上次选区执行命令（`:'<,'>s/...`）。 |

    ---

    ### 场景模拟：它们是如何工作的？

    假设你在编辑这段代码：

    ```cpp
    void func_1() { return 0; }

    ```

    #### 第一阶段：正在选中 (Visual Mode)

    1. 你按下 `v` 并移动到 `)`。
    2. **此时此刻：** * ``[` 和 ``]` 还是**旧的**（指向你上一次复制或粘贴的地方）。
    * ``<` 和 ``>` 也是**旧的**（指向你上一次 Visual 选区）。
    * **唯一**能实时跳到 `v` 的只有 **`o`**。



    #### 第二阶段：退出选区 (Exit Visual)

    1. 你按下 `Esc` 退出。
    2. **快门按下：** ``<` 瞬间更新为 `v` 的位置，``>` 更新为 `)` 的位置。
    3. **此时：** 你输入 **`gv`**，Vim 其实就是去读这两个标记，帮你把区域重新刷黑。

    #### 第三阶段：执行操作 (After Operator)

    1. 你重新选中区域并按下 **`y`** (Yank)。
    2. **快门再次按下：** * ``<` 和 ``>` 依然保留（因为选区结束了）。
    * **``[` 和 ``]` 被更新**，记录下被复制内容的范围。


    3. **应用：** 假如你执行的是 `p` (Paste) 了一大段文档，按 **``]`** 就能让你瞬间从第一行跳到粘贴完的最后一行。

    ---

    ### 一个极实用的进阶技巧：自定义快捷键

    如果你觉得在 Visual 模式下只能用 `o` 来回跳，而无法像普通模式那样精准定位边界，你可以在 `.vimrc` 中利用这些标记实现“**回到选区开头**”：

    ```vim
    " 在 Visual 模式下，按 \ [ 跳到当前选区的开头（虽然还没更新快门，但可以强制跳转）
    vnoremap <leader>[ <Esc>`<v`>o

    ```

    *注：这类映射通常比较 hack，最推荐的依然是习惯 `o` 的“旋转”逻辑。*

    ---

    ### 总结

    * **正在选：** 用 `o`。
    * **选完了想回味：** 用 ``<` 或 `gv`。
    * **动了代码（删/改/贴）后：** 用 ``[`。

    **你想了解如何利用这些标记（Marks）编写简单的自动化宏（Macro），来批量处理被选中的代码块吗？**

* let @s = '^[`<i**'
    为什么我 virual 选中 void func_1() 并执行 @s 后，变成了这样：
    struct A
    {
    **void func_1()
        {
            return 0;
        }

    这是一个非常经典的 Vim 宏（Macro）行为与缩进机制冲突的问题。

    你的宏 `let @s = '^[`<i'`逻辑上是：退出视觉模式 -> 跳到刚才选区的开头 -> 插入`**`**。

    导致你看到“缩进消失”且结果不理想的原因主要有以下三点：

    ### 1. 自动缩进（Indent）的干扰

    这是最直接的原因。在 C 风格的文件中，Vim 通常开启了 `cindent` 或 `smartindent`。

    * 当你通过宏在行首（或带有缩进的行）插入 `` 时，Vim 的缩进引擎会重新计算该行的位置。
    * 因为 `` 在 C 语言中不符合正常的语句缩进逻辑（它看起来像注释块的开始），Vim 可能会错误地认为这行应该回退到顶格（第 0 列），从而**吃掉了你原本前面的空格**。

    ### 2. 标记（Marks）更新的时机

    你在宏的开头用了 `^[` (Escape)。

    * **正确的一点**：在 Visual 模式下按 `Esc`，Vim 会立刻将当前选区的起点和终点存入 ``<` 和 ``>` 标记中。所以你的宏能精准找到 `v`。
    * **风险点**：如果你在定义宏时，`^[` 不是真正的 **Escape 键**（在脚本里应该是 `\<Esc>` 或通过 `Ctrl-V Esc` 输入），宏就会失效。

    ### 3. `i` 与 `I` 的区别

    * 在 Visual 模式下，如果你想在选区每一行开头插入字符，通常推荐用大写的 **`I`**。
    * 但你现在的宏是先退出了 Visual 模式，变成了普通模式下的 `i`。普通模式下的 `i` 只是在当前光标处插入。

    ---

    ### 更好的解决方案

    如果你想给选中的内容（如 `void func_1()`）前后加上 ``（类似 Markdown 的加粗），建议使用以下更“Vim 化”的方法：

    #### 方法 A：使用 `c` (修改) 配合寄存器（最稳妥）

    这个方法不会触发复杂的缩进重算，因为它把整个内容当作一个替换块。

    ```vim
    " 逻辑：删除选区进入插入模式 -> 输入 ** -> 粘贴刚才删的内容 -> 输入 **
    let @s = 'c****^[hp'

    ```

    *注：这里的 `^[ ` 是通过 `Ctrl-V` 然后 `Esc` 输入的，或者在 .vimrc 中写 `let @s = "c\<C-r>\""*`

    #### 方法 B：针对你原始宏的修正（防止缩进坍塌）

    如果你坚持用插入模式，可以强制 Vim 不去重新调整缩进：

    ```vim
    " 在 i 之前加上 :set paste (防止缩进干扰) 之后恢复
    let @s = ":set paste\<CR>`<i**\<Esc>:set nopaste\<CR>"

    ```

    #### 方法 C：使用插件（终极方案）

    如果你经常需要这种操作，强烈建议安装 **`vim-surround`** 插件。

    * **操作**：选中内容后，直接按 `S*`。
    * 它会自动处理所有的边界和缩进问题，非常优雅。

    ---

    ### 调试建议

    你可以尝试在执行宏之前，先执行 `:set nocindent` 和 `:set nosmartindent`。如果此时执行 `@s` 缩进没有消失，那就确认是 Vim 的自动缩进引擎在插入 `` 时“自作聪明”地把你的代码往左推了。

    **你想让我演示一下如何编写一个能自动在选区两端同时加上 `` 且不破坏格式的更高级函数吗？**

* 客观中立地评价下，现在的ai是泡沫还是风口

    关于现在的人工智能是泡沫还是风口，这是一个非常复杂的问题，没有一个简单的答案。我们可以从不同的角度来客观地分析一下：

    ---

    ### 支持“风口”的观点

    1. **技术进步迅猛：** 近年来，AI在自然语言处理（NLP）、计算机视觉和机器学习等领域的进步是前所未有的。以ChatGPT为代表的大语言模型（LLM）的出现，让AI的能力以一种直观、可交互的方式呈现在大众面前，极大地推动了应用场景的拓展。
    2. **广泛的应用前景：** AI正在渗透到各行各业，从医疗诊断、金融风控到自动驾驶、智能制造。它正在提升效率、降低成本，并创造新的商业模式。许多传统行业正在通过AI进行数字化转型，这表明AI的价值被广泛认可。
    3. **巨大的投资和人才涌入：** 全球顶尖科技公司、风险投资机构都在向AI领域投入巨资，这不仅仅是资金的追逐，更是对未来发展潜力的认可。同时，大量顶尖科学家和工程师投身其中，为技术突破提供了坚实的人才基础。
    4. **基础设施的完善：** 算力、数据、算法这三大要素正在以前所未有的速度发展。高性能计算芯片（如GPU）的进步、云计算服务的普及以及海量数据的积累，都为AI的持续发展提供了强大的基础支撑。

    ---

    ### 支持“泡沫”的观点

    1. **估值过高：** 许多AI初创公司的估值高得惊人，但其商业模式和盈利能力尚不清晰。这种高估值很大程度上是基于对未来的预期，而不是当前的实际业绩。这让人联想到2000年的互联网泡沫，当时许多互联网公司也因前景被无限看好而估值虚高，最终破灭。
    2. **技术落地挑战：** 尽管AI技术很先进，但将其成功应用于实际场景并实现规模化盈利，依然面临许多挑战。例如，**数据隐私**、**伦理问题**、**技术可靠性**以及与现有系统的集成难度等，都可能阻碍其大规模商业化。
    3. **同质化竞争：** 许多公司都在追逐相同的AI热点（如大语言模型），导致产品和服务高度同质化。这种情况下，如果没有独特的技术壁垒或清晰的商业策略，很容易陷入价格战或被市场淘汰。
    4. **公众期望与实际能力差距：** 当前媒体和公众对AI的宣传和期望有时超出了其当前阶段的实际能力。人们可能认为AI无所不能，但实际上它在很多复杂任务上依然表现不佳，且容易出现“幻觉”（hallucination）或不可预测的错误。当这种期望落差累积到一定程度，可能会导致市场的信心危机。

    ---

    ### 综合评价与结论

    从客观角度来看，目前的AI既有泡沫的成分，也具备风口的特质。

    * **泡沫的部分** 主要体现在资本市场的非理性繁荣和一些公司估值的虚高上。这是一种市场情绪的体现，就像任何新技术浪潮初期一样，总会有盲目跟风和过度炒作。
    * **风口的部分** 则体现在AI作为一种**底层技术**，正在深刻地改变生产力和社会结构。它的技术进步是真实的，应用价值是巨大的，长期潜力是毋庸置疑的。

    因此，更准确的说法是：**AI是一个巨大的、正在形成的“风口”，但在这个风口之上，存在着局部的“泡沫”**。

    对于普通投资者或从业者来说，关键在于识别泡沫与风口。真正有价值的AI公司，应该是那些拥有核心技术壁垒、清晰的商业模式，并能解决实际痛点，而不仅仅是追逐热点的公司。AI的未来发展，很可能会经历一次去伪存真的过程，洗去那些没有实质价值的泡沫，让真正的技术和应用脱颖而出。

* 我是一名投资者，我被要求看一个AI玩具的项目，我不清楚是否应该投钱，但是我没有时间调研了，请给我一个简洁的回答

    投资决策需要全面的研究，但是如果你没有时间，以下是一些关键点的简洁分析，可以帮助你快速判断。

    **AI玩具的利与弊**

    优势：

    * **市场潜力大：** 结合了AI技术和玩具市场，具有巨大的想象空间。
    * **新奇感：** AI互动可以为传统玩具带来新功能，例如语音识别、智能对话、个性化学习等。
    * **高溢价：** AI概念可以提升产品的价格，增加利润空间。

    劣势：

    * **技术门槛高：** 需要强大的研发团队，从语音识别到芯片集成，技术链很长。
    * **商业模式不清晰：** 很多AI玩具只是噱头，缺乏持续的盈利能力和用户粘性，一旦新鲜感过去，很快就会被市场淘汰。
    * **同质化严重：** 如果技术没有壁垒，很容易被其他公司复制。
    * **数据和隐私风险：** 涉及到儿童数据，需要严格遵守相关法规，否则会面临法律风险。

    **投资建议：**

    如果你没有时间深入调研，可以从以下三个核心问题来快速评估这个项目：

    1. 产品是“真AI”还是“假AI”？

        * 真AI： 具有强大的学习能力和个性化交互，可以随着使用时间变得“更聪明”。例如，一个能记住孩子喜好并提供定制化故事的玩具。
        * 假AI： 只是预设了简单的语音指令和固定回答。这类产品没有核心技术，生命周期短。

    2. 公司的护城河（核心竞争力）是什么？

        * **技术壁垒：** 是否拥有独家的AI算法、语音模型或芯片技术？
        * **品牌：** 在儿童玩具市场是否有知名度？
        * **渠道：** 是否有强大的分销渠道，能迅速将产品推向市场？

    3. 商业模式是否可持续？

        * **一次性销售：** 仅仅靠卖玩具赚钱，容易陷入价格战。
        * **订阅制或生态系统：** 是否提供付费内容、新故事或新的游戏模式？这种模式能带来持续的现金流，增强用户粘性。

    **结论**

    **如果你只关注“是否投钱”这个问题，那么最简单的判断标准就是：这个项目是否具备真正的技术壁垒和可持续的商业模式。**

    如果这个AI玩具只是简单地加了一个语音模块，没有独特的技术或后续服务，那么它很可能只是一个短暂的“泡沫”，投资风险较高。反之，如果它拥有强大的AI技术，能够持续更新内容，并建立起一个生态系统，那么它更有可能成为一个成功的“风口”项目。

    你可以根据上述要点，向项目方提出尖锐的问题，从他们的回答中快速判断这个项目的真实价值。

* 他们想做桌面剧场，结合人型机器人技术，手办技术，AI技术，让手办动起来，演一幕幕的舞台剧

    这是一个非常有创意，同时又风险极高的项目。考虑到你没有时间调研，以下是我的核心观点：

    这个项目是一个**高风险、高回报**的“风口”项目，但它能否成功，取决于以下三个关键因素：

    1. 核心技术壁垒

        * **风险：** 仅仅让手办“动起来”的技术门槛相对较低。如果只是简单的编程动作，很容易被模仿。
        * **关键问题：** 他们的AI技术能达到什么水平？是能根据剧本实时生成动作和对话，还是仅仅预设好的表演？如果是后者，那它更像是一个高端玩具，而不是一个有持续生命力的AI产品。真正有价值的AI，应该能做到“千变万化”，让用户每次观看都有新的体验。

    2. 内容生态的可持续性

        * **风险：** 舞台剧需要持续的剧本和内容更新。如果内容供给跟不上，用户很快就会失去兴趣，变成一次性消费品。
        * **关键问题：** 他们的商业模式是什么？是靠卖手办赚钱，还是有内容订阅服务？他们是否有强大的内容创作团队或合作方，能不断推出新的“剧目”？如果他们能建立一个内容平台，让用户或创作者也能参与制作和分享，那将大大增加项目的生命力。

    3. 市场定位与成本

        * **风险：** 这个产品结合了多种技术，成本必然不低。它的目标用户是谁？是普通消费者还是小众的收藏家？高昂的价格可能会限制其市场规模。
        * **关键问题：** 他们如何控制成本，并确保产品价格在目标用户能接受的范围内？他们如何教育市场，让消费者理解并愿意为这种“桌面剧场”的体验付费？

    **结论:**

    **这个项目如果成功，将是一个巨大的“风口”；如果失败，则是一个典型的“泡沫”。**

    * **如果**他们的AI能真正实现智能互动，并能通过订阅或UGC（用户生成内容）模式持续提供内容，那么它很可能是一个颠覆性的产品。
    * **如果**它只是一个卖高价、动作固定的“智能手办”，那么它就是一个短期的“泡沫”，在新鲜感过后，价值会迅速下降。

    **建议你直接问他们：** “你们如何确保这不是一个一次性消费品，而是能让用户持续付费的内容平台？” 这个问题能帮你快速洞察这个项目的核心价值所在。

* 他们打算编程实现动作和剧本，但是程序和剧本可以付费OTA下载，每两个月发布一个新剧本

    这个新信息非常关键，它改变了项目的本质。

    这个项目不再是一个依赖**AI技术**驱动的“风口”**项目，而是一个以**硬件为载体、以内容为核心的商业模式。

    **重新评估：项目的优劣势**

    * 优势：商业模式更清晰

        * **持续收入：** 付费下载新剧本的模式创造了持续的收入流，而非一次性销售。这比单纯卖硬件要健康得多，也更容易估值。
        * **可控性高：** 预先编程的剧本能确保表演的质量和稳定性，避免了生成式AI可能出现的“幻觉”和错误，用户体验更可靠。

    * 劣势：核心竞争力有待验证

        * **技术门槛低：** 这种模式的“AI”技术含量并不高，本质上只是一个高级的远程控制玩具。只要能做出类似的机器人硬件，竞争对手很容易模仿这种模式。
        * **内容是生命线：** 项目的成败完全取决于他们能否持续创作出高质量、吸引人的剧本。如果剧本乏味或更新太慢，用户就不会付费，整个商业模式就会崩溃。
        * **留存率风险：** 用户是否愿意每两个月付费一次来观看新剧本？如果剧本数量有限或缺乏新意，用户的新鲜感很快就会过去。

    **投资结论**

    现在，你的投资决策不再是押注在AI技术的未来上，而是押注在他们**内容创作**的能力和**用户运营**的能力上。

    你可以把这个项目理解为：**一家以机器人手办为载体的“内容公司”**。

    投资前的核心问题就变成了：

    * **他们的内容团队有多强？** 是否有优秀的编剧或故事创作者？
    * **他们的第一批剧本有多吸引人？** 能否让用户产生强烈的付费意愿？
    * **他们如何推广内容？** 如何确保用户在两个月后依然保持兴趣？

    如果他们能让你相信，他们有能力源源不断地创造出精彩的剧本，并且能有效触达目标用户，那这个项目就值得投资。反之，如果他们的内容规划模糊不清，那它就只是一个华而不实、缺乏持续生命力的产品。

    **机会永远都在，错过一个不可惜。**

    * 投资成功与否，并不在于你是否抓住了每一个机会，而在于你是否抓住了**适合你**的好机会。
    * 如果一个项目需要用这种方式来让你感到压力，那它本身就存在问题。

* 现在社会生产力的效率很高了，AI 只提升工作效率会造成大量人员失业，只有让AI搞情感陪伴和娱乐玩具，才是蓝海市场

* vscode 插件：CJK-Word-Break

    它强制将所有 CJK 字符边界识别为词语分隔符。
