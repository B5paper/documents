* [v] vim omni

    ```vim
    " 自动触发补全
    set completeopt=menuone,noinsert,noselect
    set shortmess+=c  " 避免补全菜单显示消息

    " 映射快捷键
    inoremap <expr> <Tab> pumvisible() ? "\<C-n>" : "\<Tab>"
    inoremap <expr> <S-Tab> pumvisible() ? "\<C-p>" : "\<S-Tab>"
    inoremap <expr> <CR> pumvisible() ? "\<C-y>" : "\<CR>"
    ```

* [v] 处理 2 条 log

* [v] np.count_nonzero()

* [v] np.stack

* [v] 处理 2 条 log

* [v] scipy 1d 卷积 py code

* [ ] 处理 2 条 log

* [ ] use sklearn regression model to predict iris class and sin curve

* [ ] 方法五：使用 pyproject.toml（现代Python项目推荐）

    创建 `pyproject.toml` 文件：
    ```toml
    [build-system]
    requires = ["setuptools", "wheel"]

    [tool.setuptools.packages.find]
    where = ["."]
    include = ["*"]
    ```

* os.path.abspath()

    os.path.join()

    sys.path

    os.path.dirname()

    os.path.exists()

    sys.path.insert()

    os.chdir()

* [ ] set -a, set +a

* [ ] python-dotenv

* [ ] git cherry-pick

* [ ] cygpath

* [ ] 

    ```bash
    sudo usermod -L admin
    sudo passwd -l admin
    ```

    或者直接删除：

    ```bash
    sudo userdel -r admin
    ```


    ```bash
    ip neigh | grep 100.104.94.244
    ```

    ```bash
    arp -an | grep 100.104.94.244
    ```

* [ ] direnv

* 为什么 std::hash<string>()(p.name) 无法自动推导 p.name 类型？

* 为什么 decltype(&calc_hash) 要加 & 才行？

* nl 命令

* [ ] 查看 amd ccl 的单元测试组织方式

* [ ] 整理 ccl topo test case 部分，并上传

* [ ] ccl 绕过 check p2p

* [ ] sudo sh -c "echo 1 > /sys/class/iommu/dmar*/reset"

* [ ] iommu=relaxable iommu.forcedac=0 iommu.strict=0

* [ ] `echo "vfio-pci" > /sys/bus/pci/devices/0000:01:00.0/driver_override`

* [ ] `lspci -nnk -s 01:00.0`

* [ ] `ls -l /sys/bus/pci/devices/0000:01:00.0/iommu_group/devices/`

* [ ] 继续精简 local res 的功能

* [ ] 对比 local res 和 nccl 229 xml 对应的功能