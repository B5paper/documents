* fail2ban 如何测试指定一个字符串是否被 filter 匹配？

    在fail2ban中，你可以使用`fail2ban-regex`工具来测试指定的字符串是否被filter匹配。以下是几种测试方法：

    ## 1. **基本测试语法**
    ```bash
    fail2ban-regex "要测试的日志字符串" /path/to/filter.conf
    ```

    ## 2. **测试示例**

    **示例1：测试单行日志**
    ```bash
    # 测试单行日志是否匹配
    fail2ban-regex "2023-10-01 12:34:56 Failed password for root from 192.168.1.100" /etc/fail2ban/filter.d/sshd.conf
    ```

    **示例2：测试多行日志**
    ```bash
    # 创建测试文件
    echo "2023-10-01 12:34:56 Failed password for root from 192.168.1.100" > test.log
    echo "2023-10-01 12:35:01 Failed password for admin from 192.168.1.101" >> test.log

    # 测试文件
    fail2ban-regex test.log /etc/fail2ban/filter.d/sshd.conf
    ```

    ## 3. **使用不同格式的输入**

    **从标准输入测试：**
    ```bash
    echo "Invalid user test from 192.168.1.100" | fail2ban-regex stdin /etc/fail2ban/filter.d/sshd.conf
    ```

    **从变量测试：**
    ```bash
    LOG_LINE="Connection closed by invalid user [preauth]"
    echo "$LOG_LINE" | fail2ban-regex stdin /etc/fail2ban/filter.d/sshd.conf
    ```

    ## 4. **详细输出模式**

    **显示详细信息：**
    ```bash
    # -v 显示详细信息
    fail2ban-regex -v "测试字符串" /etc/fail2ban/filter.d/sshd.conf

    # --print-all-matched 显示所有匹配
    fail2ban-regex --print-all-matched "测试字符串" /path/to/filter.conf

    # --print-ignored 显示被忽略的行
    fail2ban-regex --print-ignored "测试字符串" /path/to/filter.conf
    ```

    ## 5. **常用选项**

    | 选项 | 说明 |
    |------|------|
    | `-v` | 详细输出，显示匹配过程 |
    | `--print-all-matched` | 显示所有匹配的行 |
    | `--print-ignored` | 显示被忽略的行 |
    | `--print-all-missed` | 显示所有未匹配的行 |
    | `-d <DATE>` | 设置日期模式 |
    | `--full-trace` | 显示完整的调试信息 |

    ## 6. **实际应用示例**

    ```bash
    # 测试SSH失败登录
    fail2ban-regex "Jan 1 12:34:56 server sshd[1234]: Failed password for invalid user test from 192.168.1.100 port 22 ssh2" /etc/fail2ban/filter.d/sshd.conf

    # 测试自定义filter
    fail2ban-regex "ERROR - Authentication failed for user=admin" /path/to/custom-filter.conf

    # 显示详细匹配信息
    fail2ban-regex -v --print-all-matched "测试字符串" /etc/fail2ban/filter.d/apache-auth.conf
    ```

    ## 7. **在脚本中使用**

    你可以将`fail2ban-regex`整合到脚本中进行自动化测试：

    ```bash
    #!/bin/bash

    TEST_STRING="Failed password for root from 192.168.1.100"
    FILTER="/etc/fail2ban/filter.d/sshd.conf"

    if fail2ban-regex "$TEST_STRING" "$FILTER" > /dev/null 2>&1; then
        echo "字符串匹配成功！"
    else
        echo "字符串不匹配。"
    fi
    ```

    ## 8. **调试filter规则**

    如果你需要调试或测试filter的某个特定正则表达式：

    ```bash
    # 直接测试正则表达式
    fail2ban-regex "测试字符串" 'failregex = ^.*Failed password.*<HOST>'
    ```

    ## 注意事项：
    1. 确保fail2ban服务已安装：`apt install fail2ban` 或 `yum install fail2ban`
    2. filter文件路径通常为：`/etc/fail2ban/filter.d/`
    3. 测试时会考虑filter中的`failregex`和`ignoreregex`配置
    4. 输出结果会显示匹配的主机IP、时间和匹配次数等信息

    通过`fail2ban-regex`工具，你可以快速验证filter规则是否正确匹配特定的日志字符串，这对于调试和优化fail2ban配置非常有帮助。

* [hlc@iZuf63p69virk2qhn3bl4jZ ~]$ fail2ban-regex "12月 02 20:52:30 iZuf63p69virk2qhn3bl4jZ sshd[1753315]: Connection closed by authenticating user root 167.172.36.246 port 56310 [preauth]" /etc/fail2ban/filter.d/sshd.conf 

Running tests
=============

Use   failregex filter file : sshd, basedir: /etc/fail2ban
Use         maxlines : 1
Use      datepattern : {^LN-BEG} : Default Detectors
Use      single line : 12月 02 20:52:30 iZuf63p69virk2qhn3bl4jZ sshd[17533...


Results
=======

Prefregex: 1 total
|  ^(?P<mlfid>(?:\[\])?\s*(?:<[^.]+\.[^.]+>\s+)?(?:\S+\s+)?(?:kernel:\s?\[ *\d+\.\d+\]:?\s+)?(?:@vserver_\S+\s+)?(?:(?:(?:\[\d+\])?:\s+[\[\(]?sshd(?:\(\S+\))?[\]\)]?:?|[\[\(]?sshd(?:\(\S+\))?[\]\)]?:?(?:\[\d+\])?:?)\s+)?(?:\[ID \d+ \S+\]\s+)?)(?:(?:error|fatal): (?:PAM: )?)?(?P<content>.+)$
`-

Failregex: 0 total

Ignoreregex: 0 total

Date template hits:

Lines: 1 lines, 0 ignored, 0 matched, 1 missed
[processed in 0.01 sec]

|- Missed line(s):
|  12月 02 20:52:30 iZuf63p69virk2qhn3bl4jZ sshd[1753315]: Connection closed by authenticating user root 167.172.36.246 port 56310 [preauth]
`-
[hlc@iZuf63p69virk2qhn3bl4jZ ~]$ 

这是啥意思

    从你的测试结果来看，**这条日志没有被匹配**。让我详细解释一下：

    ## 结果分析：

    ### 关键信息：
    1. **Failregex: 0 total** - 表示没有找到匹配的failregex规则
    2. **Lines: 1 lines, 0 ignored, 0 matched, 1 missed** - 1行日志，0行被忽略，0行匹配，1行未匹配
    3. **Missed line(s):** - 下面列出了未匹配的行

    ### 为什么没有匹配？
    你的日志行是：
    ```
    Connection closed by authenticating user root 167.172.36.246 port 56310 [preauth]
    ```

    标准的sshd filter主要匹配**失败认证**的日志，比如：
    - `Failed password for`
    - `Invalid user`
    - `authentication failure`

    而你的日志是`Connection closed by authenticating user`，这通常表示**认证成功后连接关闭**，不是失败登录尝试。

    ## 验证其他类型的日志：

    让我们测试一下常见的失败登录日志：

    ```bash
    # 1. 测试失败密码
    fail2ban-regex "Failed password for root from 167.172.36.246 port 56310 ssh2" /etc/fail2ban/filter.d/sshd.conf

    # 2. 测试无效用户
    fail2ban-regex "Invalid user test from 167.172.36.246 port 56310" /etc/fail2ban/filter.d/sshd.conf

    # 3. 测试PAM认证失败
    fail2ban-regex "PAM authentication error for root from 167.172.36.246" /etc/fail2ban/filter.d/sshd.conf
    ```

    ## 查看sshd filter的规则：

    如果你想了解哪些日志会被匹配，可以查看filter文件：

    ```bash
    # 查看sshd的匹配规则
    grep -A5 "failregex" /etc/fail2ban/filter.d/sshd.conf | head -20

    # 或者查看所有failregex规则
    grep "^failregex" /etc/fail2ban/filter.d/sshd.conf -A10
    ```

    ## 测试真正的攻击日志：

    这里有几个会被fail2ban匹配的示例：

    ```bash
    # 这些通常会被匹配：
    fail2ban-regex "Failed password for invalid user admin from 167.172.36.246 port 56310 ssh2" /etc/fail2ban/filter.d/sshd.conf

    fail2ban-regex "error: maximum authentication attempts exceeded for root from 167.172.36.246 port 56310 ssh2 [preauth]" /etc/fail2ban/filter.d/sshd.conf

    fail2ban-regex "Received disconnect from 167.172.36.246 port 56310:11: Bye Bye [preauth]" /etc/fail2ban/filter.d/sshd.conf
    ```

    ## 你的日志是什么意思？

    `Connection closed by authenticating user root 167.172.36.246 port 56310 [preauth]`

    这行日志表示：
    - 用户`root`正在尝试认证（authenticating user）
    - 来自IP `167.172.36.246` 端口 `56310`
    - 连接在认证前被关闭（[preauth]）
    - 这**不是**密码失败的记录，而是连接在认证完成前被中断

    ## 总结：

    你的测试结果是**正常的** - 因为这条日志不是失败登录尝试，所以没有被fail2ban的sshd filter匹配。Fail2ban主要监控的是**认证失败**的日志，而不是所有连接相关的日志。

    如果你看到大量的这种日志，可能需要检查：
    1. 是否是网络问题导致连接中断
    2. 是否有客户端配置问题
    3. 如果来自同一个IP的大量此类日志，可能需要自定义filter来监控

* sshd.conf 里有这几行：
failregex = %(cmnfailre)s
            <mdre-<mode>>
            %(cfooterre)s
            ^%(__prefix_line)sConnection reset by authenticating user root <HOST> port \d+ \[preauth\]$
            ^%(__prefix_line)sConnection closed by authenticating user root <HOST> port \d+ \[preauth\]$
这也不行吗？

    你提到的情况确实有些奇怪。根据你的配置，sshd.conf 中确实包含了匹配这种日志的规则，但测试结果显示没有匹配。让我帮你分析原因：

    ## 问题分析：

    从你的配置看，应该能匹配：
    ```
    ^%(__prefix_line)sConnection closed by authenticating user root <HOST> port \d+ \[preauth\]$
    ```

    但测试结果却是 `Missed`。可能有以下几个原因：

    ## 1. **测试完整的filter文件**

    先确认你正在测试的文件是否就是实际生效的文件：

    ```bash
    # 1. 查看文件路径确认
    ls -la /etc/fail2ban/filter.d/sshd.conf

    # 2. 查看实际匹配规则
    grep -n "Connection closed by authenticating user" /etc/fail2ban/filter.d/sshd.conf

    # 3. 使用完整路径测试
    fail2ban-regex "12月 02 20:52:30 iZuf63p69virk2qhn3bl4jZ sshd[1753315]: Connection closed by authenticating user root 167.172.36.246 port 56310 [preauth]" /etc/fail2ban/filter.d/sshd.conf -v
    ```

    ## 2. **查看变量定义**

    问题可能在于 `%(__prefix_line)s` 这个变量。查看它的定义：

    ```bash
    # 查看 __prefix_line 的定义
    grep -r "__prefix_line" /etc/fail2ban/filter.d/sshd.conf
    grep -r "__prefix_line" /etc/fail2ban/filter.d/common.conf
    ```

    ## 3. **直接测试原始正则表达式**

    让我们绕过变量，直接测试原始正则表达式：

    ```bash
    # 提取实际的匹配模式
    PREFIX='^.*?sshd(?:\(\S+\))?:?\s*'
    TEST_STRING="12月 02 20:52:30 iZuf63p69virk2qhn3bl4jZ sshd[1753315]: Connection closed by authenticating user root 167.172.36.246 port 56310 [preauth]"

    # 使用简化模式测试
    fail2ban-regex "$TEST_STRING" 'failregex = Connection closed by authenticating user root <HOST> port \d+ \[preauth\]$'

    # 或者测试不带时间戳
    fail2ban-regex "sshd[1753315]: Connection closed by authenticating user root 167.172.36.246 port 56310 [preauth]" /etc/fail2ban/filter.d/sshd.conf
    ```

    ## 4. **查看变量实际值**

    创建一个小脚本来查看变量的实际替换值：

    ```bash
    cat > test_filter.conf << 'EOF'
    [Definition]
    __prefix_line = ^\s*(?:\S+ )?%(__preamble)s(?:\[%(__pid_re)s?\])?\s*
    failregex = ^%(__prefix_line)sConnection closed by authenticating user root <HOST> port \d+ \[preauth\]$
    ignoreregex =
    EOF

    # 使用自定义配置测试
    fail2ban-regex "12月 02 20:52:30 iZuf63p69virk2qhn3bl4jZ sshd[1753315]: Connection closed by authenticating user root 167.172.36.246 port 56310 [preauth]" test_filter.conf -v
    ```

    ## 5. **可能是日期格式问题**

    你的日志包含中文字符的月份 `12月`，这可能影响匹配：

    ```bash
    # 测试不带日期的简化版本
    fail2ban-regex "sshd[1753315]: Connection closed by authenticating user root 167.172.36.246 port 56310 [preauth]" /etc/fail2ban/filter.d/sshd.conf

    # 或者测试英文月份
    fail2ban-regex "Dec 02 20:52:30 iZuf63p69virk2qhn3bl4jZ sshd[1753315]: Connection closed by authenticating user root 167.172.36.246 port 56310 [preauth]" /etc/fail2ban/filter.d/sshd.conf
    ```

    ## 6. **查看完整的正则表达式**

    让我们查看实际的匹配模式：

    ```bash
    # 显示详细的匹配过程
    fail2ban-regex -v --full-trace "Connection closed by authenticating user root 167.172.36.246 port 56310 [preauth]" /etc/fail2ban/filter.d/sshd.conf
    ```

    ## 7. **临时修改配置测试**

    ```bash
    # 备份原配置
    cp /etc/fail2ban/filter.d/sshd.conf /etc/fail2ban/filter.d/sshd.conf.backup

    # 创建简化的测试配置
    cat > /tmp/test_sshd.conf << 'EOF'
    [Definition]
    failregex = Connection closed by authenticating user root <HOST> port \d+ \[preauth\]$
    ignoreregex =
    EOF

    # 测试简化版本
    fail2ban-regex "Connection closed by authenticating user root 167.172.36.246 port 56310 [preauth]" /tmp/test_sshd.conf

    # 恢复配置
    cp /etc/fail2ban/filter.d/sshd.conf.backup /etc/fail2ban/filter.d/sshd.conf
    ```

    ## 8. **使用更具体的测试**

    ```bash
    # 只测试关键部分
    echo "Connection closed by authenticating user root 167.172.36.246 port 56310 [preauth]" | \
        fail2ban-regex stdin /etc/fail2ban/filter.d/sshd.conf
    ```

    ## 可能的原因：

    1. **日期格式问题**：中文字符月份可能不在预期的日期模式中
    2. **前缀不匹配**：`%(__prefix_line)s` 可能没有正确匹配你的日志前缀
    3. **空格问题**：日志中的空格可能与正则表达式中的空格不完全匹配

    ## 建议的解决方案：

    如果这确实是攻击日志，你可以：

    1. **自定义filter**：
    ```bash
    # 创建自定义filter
    sudo nano /etc/fail2ban/filter.d/sshd-custom.conf
    ```

    ```ini
    [Definition]
    failregex = ^.*Connection closed by authenticating user root <HOST> port \d+ \[preauth\]$
    ignoreregex =
    ```

    2. **修改sshd配置**：
    ```bash
    # 在现有的sshd.conf中添加更宽松的匹配
    sudo nano /etc/fail2ban/filter.d/sshd.conf
    ```

    在failregex部分添加：
    ```
    ^.*Connection closed by authenticating user root <HOST> port \d+ \[preauth\]$
    ```

    请尝试上述测试方法，告诉我哪一步的结果，我可以帮你进一步分析。

* 如何查看它提取出的 <HOST> 具体是多少？

    你可以使用 `--print-all-matched` 选项来查看提取的 `<HOST>` 值。具体方法如下：

    ## 1. **查看匹配的HOST值**

    ```bash
    # 使用 --print-all-matched 显示匹配详情
    fail2ban-regex --print-all-matched \
        "12月 02 20:52:30 iZuf63p69virk2qhn3bl4jZ sshd[1753315]: Connection closed by authenticating user root 167.172.36.246 port 56310 [preauth]" \
        /etc/fail2ban/filter.d/sshd.conf
    ```

    ## 2. **显示更详细的信息**

    ```bash
    # 使用详细模式查看提取过程
    fail2ban-regex -v --print-all-matched \
        "12月 02 20:52:30 iZuf63p69virk2qhn3bl4jZ sshd[1753315]: Connection closed by authenticating user root 167.172.36.246 port 56310 [preauth]" \
        /etc/fail2ban/filter.d/sshd.conf
    ```

    ## 3. **如果上面的不显示，尝试这个调试方法**

    创建一个简化的测试配置文件：

    ```bash
    # 创建测试配置
    cat > /tmp/debug_host.conf << 'EOF'
    [Definition]
    failregex = Connection closed by authenticating user root <HOST> port \d+ \[preauth\]$
    ignoreregex =
    EOF

    # 测试并显示HOST
    fail2ban-regex --print-all-matched \
        "Connection closed by authenticating user root 167.172.36.246 port 56310 [preauth]" \
        /tmp/debug_host.conf
    ```

    ## 4. **使用grep查看匹配过程**

    ```bash
    # 首先确保能匹配
    echo "Connection closed by authenticating user root 167.172.36.246 port 56310 [preauth]" | \
        fail2ban-regex stdin /etc/fail2ban/filter.d/sshd.conf -v
    ```

    ## 5. **如果上面方法都不行，使用正则表达式测试**

    ```bash
    # 直接测试正则表达式提取
    echo "Connection closed by authenticating user root 167.172.36.246 port 56310 [preauth]" | \
        python3 -c "
    import re
    import sys
    line = sys.stdin.read().strip()
    pattern = r'Connection closed by authenticating user root (\d+\.\d+\.\d+\.\d+) port \d+ \[preauth\]\$'
    match = re.search(pattern, line)
    if match:
        print('提取的HOST:', match.group(1))
    else:
        print('未匹配')
    "
    ```

    ## 6. **检查fail2ban的调试输出**

    ```bash
    # 启用更详细的调试
    fail2ban-regex --full-trace \
        "Connection closed by authenticating user root 167.172.36.246 port 56310 [preauth]" \
        /etc/fail2ban/filter.d/sshd.conf 2>&1 | grep -i host
    ```

    ## 7. **使用fail2ban-client测试**

    ```bash
    # 另一种方法是通过fail2ban服务测试
    sudo fail2ban-client -vvv start sshd 2>&1 | grep -A5 -B5 "167.172.36.246"
    ```

    ## 8. **创建测试脚本**

    ```bash
    cat > test_host_extraction.sh << 'EOF'
    #!/bin/bash

    # 测试字符串
    LOG_LINE="12月 02 20:52:30 iZuf63p69virk2qhn3bl4jZ sshd[1753315]: Connection closed by authenticating user root 167.172.36.246 port 56310 [preauth]"

    # 创建临时filter文件
    cat > /tmp/test_extract.conf << 'CONFEOF'
    [Definition]
    failregex = ^.*Connection closed by authenticating user root <HOST> port \d+ \[preauth\]$
    ignoreregex =
    CONFEOF

    echo "测试日志: $LOG_LINE"
    echo "----------------------------------------"

    # 运行测试
    fail2ban-regex --print-all-matched "$LOG_LINE" /tmp/test_extract.conf

    # 清理
    rm -f /tmp/test_extract.conf
    EOF

    chmod +x test_host_extraction.sh
    ./test_host_extraction.sh
    ```

    ## 9. **查看实际的匹配结果**

    如果`--print-all-matched`不显示具体值，可能根本没有匹配成功。你可以先确认是否有匹配：

    ```bash
    # 检查是否有任何匹配
    echo "Connection closed by authenticating user root 167.172.36.246 port 56310 [preauth]" | \
        fail2ban-regex stdin 'failregex = Connection closed by authenticating user root <HOST> port \d+ \[preauth\]$' | \
        grep -E "(Matched|HOST|IP)"
    ```

    ## 10. **使用Python直接调试**

    ```bash
    python3 << 'EOF'
    import re

    # 日志行
    line = "12月 02 20:52:30 iZuf63p69virk2qhn3bl4jZ sshd[1753315]: Connection closed by authenticating user root 167.172.36.246 port 56310 [preauth]"

    # 尝试不同的模式
    patterns = [
        r'Connection closed by authenticating user root (\d+\.\d+\.\d+\.\d+) port \d+ \[preauth\]$',
        r'root (\d+\.\d+\.\d+\.\d+) port \d+ \[preauth\]$',
        r'(\d+\.\d+\.\d+\.\d+) port \d+ \[preauth\]$',
    ]

    for i, pattern in enumerate(patterns):
        match = re.search(pattern, line)
        if match:
            print(f"模式{i+1}匹配成功!")
            print(f"提取的IP: {match.group(1)}")
            break
    else:
        print("所有模式都未匹配")
    EOF
    ```

    ## 如果还是看不到HOST值，可能是因为：

    1. **根本没有匹配成功**：查看输出中的 `matched: 0`
    2. **HOST标记没有正确识别**：确保正则表达式中有 `<HOST>` 标记
    3. **IP地址格式不被识别**：确认IP地址是有效的IPv4格式

    请运行上面的命令，把输出结果发给我，我可以帮你分析具体问题所在。

* [hlc@iZuf63p69virk2qhn3bl4jZ ~]$ fail2ban-regex "12月 02 20:52:30 iZuf63p69virk2qhn3bl4jZ sshd[1753315]: Connection closed by authenticating user root 167.172.36.246 port 56310 [preauth]" /etc/fail2ban/filter.d/sshd.conf  --print-all-matched

Running tests
=============

Use   failregex filter file : sshd, basedir: /etc/fail2ban
Use         maxlines : 1
Use      datepattern : {^LN-BEG} : Default Detectors
Use      single line : 12月 02 20:52:30 iZuf63p69virk2qhn3bl4jZ sshd[17533...


Results
=======

Prefregex: 1 total
|  ^(?P<mlfid>(?:\[\])?\s*(?:<[^.]+\.[^.]+>\s+)?(?:\S+\s+)?(?:kernel:\s?\[ *\d+\.\d+\]:?\s+)?(?:@vserver_\S+\s+)?(?:(?:(?:\[\d+\])?:\s+[\[\(]?sshd(?:\(\S+\))?[\]\)]?:?|[\[\(]?sshd(?:\(\S+\))?[\]\)]?:?(?:\[\d+\])?:?)\s+)?(?:\[ID \d+ \S+\]\s+)?)(?:(?:error|fatal): (?:PAM: )?)?(?P<content>.+)$
`-

Failregex: 1 total
|-  #) [# of hits] regular expression
|  24) [1] ^.*Connection closed by authenticating user root <HOST> port \d+ \[preauth\]$
`-

Ignoreregex: 0 total

Date template hits:

Lines: 1 lines, 0 ignored, 1 matched, 0 missed
[processed in 0.01 sec]

|- Matched line(s):
|  12月 02 20:52:30 iZuf63p69virk2qhn3bl4jZ sshd[1753315]: Connection closed by authenticating user root 167.172.36.246 port 56310 [preauth]
`-

    很好！现在我们可以看到 **匹配成功了**，但是输出中并没有显示提取出的 `<HOST>` 具体值。让我教你几种方法来查看提取的HOST值：

    ## 方法1：**使用 `fail2ban-regex` 的更详细输出**

    ```bash
    # 使用 -v 详细模式结合 --print-all-matched
    fail2ban-regex -v --print-all-matched \
        "12月 02 20:52:30 iZuf63p69virk2qhn3bl4jZ sshd[1753315]: Connection closed by authenticating user root 167.172.36.246 port 56310 [preauth]" \
        /etc/fail2ban/filter.d/sshd.conf
    ```

    ## 方法2：**创建调试配置查看HOST**

    ```bash
    # 创建专门用于调试的filter文件
    cat > /tmp/debug_filter.conf << 'EOF'
    [Definition]
    failregex = ^.*Connection closed by authenticating user root <HOST> port \d+ \[preauth\]$
    ignoreregex =
    EOF

    # 测试并查看HOST
    fail2ban-regex --full-trace \
        "Connection closed by authenticating user root 167.172.36.246 port 56310 [preauth]" \
        /tmp/debug_filter.conf
    ```

    ## 方法3：**使用fail2ban-python直接调试**

    ```bash
    # 使用Python的fail2ban模块
    python3 -c "
    from fail2ban.server.failregex import FailRegex
    import tempfile
    import os

    # 创建临时filter文件
    filter_content = '''[Definition]
    failregex = ^.*Connection closed by authenticating user root <HOST> port \d+ \[preauth\]$
    '''

    with tempfile.NamedTemporaryFile(mode='w', suffix='.conf', delete=False) as f:
        f.write(filter_content)
        filter_file = f.name

    try:
        # 创建FailRegex对象
        fr = FailRegex('test', filter_file)
        
        # 测试匹配
        line = 'Connection closed by authenticating user root 167.172.36.246 port 56310 [preauth]'
        matches = fr.matches(line)
        
        if matches:
            print('匹配成功！')
            print('提取的信息:')
            for match in matches:
                print(f'  HOST: {match.get(\"host\")}')
                print(f'  完整匹配: {match.get(\"matches\")}')
        else:
            print('未匹配')
            
    finally:
        os.unlink(filter_file)
    "
    ```

    ## 方法4：**查看fail2ban的实际日志**

    ```bash
    # 1. 先确保fail2ban服务运行
    sudo systemctl status fail2ban

    # 2. 查看fail2ban的日志（如果有匹配，会记录在日志中）
    sudo tail -f /var/log/fail2ban.log &

    # 3. 手动触发一次匹配（使用fail2ban-client）
    echo "12月 02 20:52:30 iZuf63p69virk2qhn3bl4jZ sshd[1753315]: Connection closed by authenticating user root 167.172.36.246 port 56310 [preauth]" | sudo tee -a /var/log/secure
    ```

    ## 方法5：**修改sshd.conf增加调试输出**

    ```bash
    # 备份原配置
    sudo cp /etc/fail2ban/filter.d/sshd.conf /etc/fail2ban/filter.d/sshd.conf.backup

    # 创建测试配置
    sudo cat > /etc/fail2ban/filter.d/sshd-debug.conf << 'EOF'
    [Definition]
    # 添加一个特殊的匹配规则，会在日志中显示
    failregex = ^.*Connection closed by authenticating user root <HOST> port \d+ \[preauth\]$
                ^.*Connection closed by authenticating user root (?P<debug_host>\d+\.\d+\.\d+\.\d+) port \d+ \[preauth\]$
    ignoreregex =
    EOF

    # 测试
    fail2ban-regex \
        "Connection closed by authenticating user root 167.172.36.246 port 56310 [preauth]" \
        /etc/fail2ban/filter.d/sshd-debug.conf -v
    ```

    ## 方法6：**使用正则表达式直接提取**

    如果你只是想知道HOST值是多少，其实可以直接看到：

    从你的日志中，HOST值很明显就是：**167.172.36.246**

    但是如果你想通过程序验证fail2ban是否能正确提取：

    ```bash
    # 查看fail2ban如何处理正则表达式
    python3 << 'EOF'
    import re

    # fail2ban处理<HOST>的方式
    line = "Connection closed by authenticating user root 167.172.36.246 port 56310 [preauth]"
    pattern = r'Connection closed by authenticating user root <HOST> port \d+ \[preauth\]$'

    # fail2ban会将<HOST>替换为匹配IP的正则表达式
    # <HOST> 对应的是: (?:[0-9]{1,3}\.){3}[0-9]{1,3}

    # 所以实际匹配的模式是：
    actual_pattern = r'Connection closed by authenticating user root (?P<host>(?:[0-9]{1,3}\.){3}[0-9]{1,3}) port \d+ \[preauth\]$'

    match = re.search(actual_pattern, line)
    if match:
        print(f"提取的HOST: {match.group('host')}")
        print(f"HOST在位置: {match.start('host')}-{match.end('host')}")
    else:
        print("未匹配")
    EOF
    ```

    ## 方法7：**查看fail2ban的匹配统计**

    ```bash
    # 重启fail2ban并查看匹配
    sudo fail2ban-client restart sshd

    # 查看匹配统计
    sudo fail2ban-client status sshd
    ```

    ## 总结：

    从你的测试结果看：
    1. ✅ **匹配成功了**（Failregex: 1 total）
    2. ✅ **规则#24被匹配**（规则编号24）
    3. ✅ **你的日志行被正确识别**

    **提取的HOST值就是：167.172.36.246**

    虽然`--print-all-matched`没有直接显示HOST值，但fail2ban在内部确实提取了这个IP地址。在实际运行时，这个IP会被添加到fail2ban的监控列表，如果达到阈值就会被ban。

    如果你想在测试时看到具体的HOST值，建议使用方法3（Python脚本）或方法6（直接正则提取）。
