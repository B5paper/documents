* [v] intel_iommu=on iommu=pt

* [v] 使用 scipy 计算两 channel 之间的 personr 相关系数

    feedback:

    1. 同一通道的 hbr, hbo 之间的相关性比不同通道的 hbr, hbr 相关性强。但是并不全为 1 或 -1，+-0.5 比较多。也有 0.9 几的，变化几乎完全相同

    2. 需要先滤波和降低漂移。尤其是漂移

        normal stroke 数据集是否已经经过降噪处理？

    3. 需要拿到任意两两 ch 之间的 hbo, hbr 的 pearsonr 相关系数 r, p，再对这些系数画出散点图，看看有什么现象

    4. 别人论文里是如何处理通道间的耦合关系的？有哪些论文？是否有 survey?

* [v] 处理 1 条 log

* [v] 洗碗

* [v] 处理 1 条 log

* [v] 使用 numpy 计算任意 channel 之间的 personr coor

    feedback:

    1. 不同尺度下的 personr 并不相同，大尺度时，漂移占主导，导致 pearsonr 趋近于 1，但是如果缩小尺度，漂移作用不明显，pearsonr 会迅速减小

    2. 即使小尺度下，也有高度相关的一些 channel。同一通道的 hbo, hbr 相关情况有，但是并不是大量。而且受漂移影响更大。

    3. 极端值会严重影响 pearsonr 系数，比如则开始的 0 值，导致所有 channel 的 pearsonr 都接近于 1。

* [v] 洗衣服

* [v] 写一个函数 inspect_chs(data, chans, time_range)，画出拆线图，并计算 chans 的 pearsonr 相关系数

    feedback:

    1. 不同 channel 中相关系数很高的 mear 数据，看到不少类似正弦曲线那样的信号，可能是心脏血流信号？看信号的频率比较像，平均 2 秒钟 3 个正弦周期。

    2. 如果要对信号做预处理，减去基线，那么是否应该再除以标准差？

* [v] 处理 1 个 log

* [v] 收拾一处屋子

* [v] 处理 1 个 log

* [v] ch 408 如何知道每个 ch 对应的 wav？ 是否有 chan list 信息？如何知道 timestamps ?

    feedback:

    1. sqlite3 数据无 chan list 信息。甚至都没有 wave length 信息。应该是全靠 csv 文件的信息了。

    2. 关于 timestamp，sqlite3 文件记录的方式是每个 timestamp 记录所有 channel 的十六进制数据，作为一帧。因此可以知道 timestamp。

    3. 这样看来，csv 数据大概也没有经过预处理，大概只是 sqlite3 的十六进制数据的转换。

    4. 关注一下 shixian liu 的论文，他的名字多次出现，可能是做 fnirs 方向的。看看他的论文里是否有对 sd layout 的介绍。

    5. 数据中的 marker 并不都为 0，目前不知道是什么含义

    6. channel rejection 是否都为 0？

* [v] 处理 1 条 log

* [v] 洗碗 + 收拾电饭锅

* [v] 处理 1 条 log

* [ ] 将创新点记录到 hd-dot doc 文档里

* [ ] 收毯子

* [ ] channel 数据之间的皮尔逊相关系数

    扩展：motion seq 之间有的皮尔逊相关系数（前 1 秒，前 2 秒，前 3 秒，前 5 秒，前 10 秒，以及可以考虑分段 block，前几秒，中间几秒，后几秒）

* [ ] use sklearn regression model to predict iris class and sin curve

* [ ] 

    ```py
    PROJECT_ROOT = Path(__file__).resolve().parents[3]
    if str(PROJECT_ROOT) not in sys.path:
        sys.path.insert(0, str(PROJECT_ROOT))
    ```

* [ ] python str glob

* [ ] csv.reader()

* [ ] rng = np.random.default_rng(), rng.shuffle()

* [ ] echo -e

* [ ] grep -q

* [ ] cut -d' ' -f1

* [ ] `dracut -f`

