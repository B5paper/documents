* [new] `struct alignas(32) ncclIbNetCommBase`中 alignas 是什么意思？

* [new] C++ 中，下面的情况如何写成智能指针？

    ```cpp
    struct A;

    struct Parent {
        A* childs[4];
    };

    struct A {
        Parent *parent;
    };
    ```

    循环引用问题？编译器不知道 struct 的大小的问题？

* [new] `ncclSocketStateConnectPolling` socket 也支持 polling 吗？

* [new] `cudaThreadExchangeStreamCaptureMode()`

* [new] `cudaPointerAttributes()`