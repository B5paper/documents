* [v] 整理 normal and stroke 的处理流程

* [v] 处理 1 个 log

* [v] scaler = StandardScaler()

    X_train_scaled = scaler.fit_transform(X_train)

    X_val_scaled = scaler.transform(X_val)

* [v] 处理 1 个 log

* [v] svm = SVC(kernel='rbf', C=1.0, gamma='scale', random_state=42)

    svm.fit(X_train_scaled, y_train)

* [v] 处理 1 个 log

* [v] accuracy_score()

* [v] 处理 1 个 log

* [v] classification_report()

* [v] 处理 1 个 log

* [ ] review vim `autocmd` 的用法，

    解释`autocmd FileType python set omnifunc=python3complete#Complete`

* [ ] 处理 1 个 log

* [ ] 整理 motion cls 的数据处理过程

* [ ] 处理 1 个 log

* [ ] normal and stroke 分析如果有了 csv 文件后，dataset.py 能否用 csv 文件的信息重写

* [ ] vim omni

    ```vim
    " 自动触发补全
    set completeopt=menuone,noinsert,noselect
    set shortmess+=c  " 避免补全菜单显示消息

    " 映射快捷键
    inoremap <expr> <Tab> pumvisible() ? "\<C-n>" : "\<Tab>"
    inoremap <expr> <S-Tab> pumvisible() ? "\<C-p>" : "\<S-Tab>"
    inoremap <expr> <CR> pumvisible() ? "\<C-y>" : "\<CR>"
    ```

* [ ] deoplete.nvim (异步补全)

    ```vim
    Plug 'Shougo/deoplete.nvim'
    Plug 'zchee/deoplete-jedi'  " Python 补全源

    let g:deoplete#enable_at_startup = 1
    ```

* [ ] 给鞋穿鞋带

* [ ] 收拾一处屋子

* [ ] 处理 2 条 log

* [ ] 洗衣服

* [ ] use sklearn regression model to predict iris class and sin curve

* [ ] 处理 2 条 log

* [ ] 方法五：使用 pyproject.toml（现代Python项目推荐）

    创建 `pyproject.toml` 文件：
    ```toml
    [build-system]
    requires = ["setuptools", "wheel"]

    [tool.setuptools.packages.find]
    where = ["."]
    include = ["*"]
    ```

* os.path.abspath()

    os.path.join()

    sys.path

    os.path.dirname()

    os.path.exists()

    sys.path.insert()

    os.chdir()

* [ ] set -a, set +a

* [ ] python-dotenv

* [ ] git cherry-pick

* [ ] cygpath

* [ ] 

    ```bash
    sudo usermod -L admin
    sudo passwd -l admin
    ```

    或者直接删除：

    ```bash
    sudo userdel -r admin
    ```


    ```bash
    ip neigh | grep 100.104.94.244
    ```

    ```bash
    arp -an | grep 100.104.94.244
    ```

* [ ] direnv

* 为什么 std::hash<string>()(p.name) 无法自动推导 p.name 类型？

* 为什么 decltype(&calc_hash) 要加 & 才行？

* nl 命令
