[processed]

* [v] hvim 在第一次补全后，`subdir/aa<tab>` 无法继续补全后续的 `subdir/aabb`，研究一下解决方案

* [v] gpu 03 机器上编译 nccl 229

* [v] 在 nv + ib 机器上 dump 带网卡数据的 xml

* [v] `echo $'hello\nworld'`

* [v] Here Document（多行文本）

    ```
    cat << EOF
    hello
    world
    EOF
    ```

    找找看之前 here doc 的笔记。
