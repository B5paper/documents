* [ ] _single_thread_get_topo_system 太长，是否可以直接使用 construct system

* [ ] hvim 在第一次补全后，`subdir/aa<tab>` 无法继续补全后续的 `subdir/aabb`，研究一下解决方案

* [ ] 在 nv + ib 机器上 dump 带网卡数据的 xml

* [ ] `echo $'hello\nworld'`

* [ ] Here Document（多行文本）

    ```
    cat << EOF
    hello
    world
    EOF
    ```

    找找看之前 here doc 的笔记。

* [ ] `mapfile -t -d $'\0' COMPREPLY < <(find . -maxdepth 1 -type f -name "*${cur}*" -print0 | sed -z 's|^\./||' | sort -z)`

* [ ] `compopt -o filenames`