* [v] normal and stroke mlp process

* [v] 处理 1 个 log

* [v] pytorch 中的 AdamW 优化器

* [v] 处理 1 个 log

* [ ] raw dataset 拿数据，打印 channel 的 matplotlib 折线

* [ ] ch 408 如何知道每个 ch 对应的 wav？ 是否有 chan list 信息？如何知道 timestamps ?

* [ ] normal and stroke 分析如果有了 csv 文件后，dataset.py 能否用 csv 文件的信息重写

* [ ] channel 数据之间的皮尔逊相关系数

    扩展：motion seq 之间有的皮尔逊相关系数（前 1 秒，前 2 秒，前 3 秒，前 5 秒，前 10 秒，以及可以考虑分段 block，前几秒，中间几秒，后几秒）

* [ ] 

* [ ] KFold / StratifiedKFold

    交叉验证，更稳定地评估模型

* [ ] TimeSeriesSplit

    时间序列数据，保证训练集在测试集之前

* [ ] ShuffleSplit

    多次随机划分，可控制训练/测试比例

* [ ] 整理 motion cls 的数据处理过程

* [v] `g@`操作符

    g@ 是 Vim 内置的操作符，专门用于在 Visual 模式下调用函数，且不会退出 Visual 模式（因为它的设计就是操作符，接受一个 motion 或 Visual 选区）。

    ```vim
    " 映射为操作符
    nnoremap <leader>fn :set opfunc=FindNextSharp<CR>g@
    vnoremap <leader>fn :<C-u>call FindNextSharp(visualmode())<CR>
    ```

* [ ] runtime 给出 ipa 映射的接口的文档
