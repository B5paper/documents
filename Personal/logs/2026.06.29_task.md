* [v] 处理 2 条 log

* [v] xml.h / .cc

    feedback:

    1. print_xml() 使用了 nccl info 来打印，后续把改成 nccL_sio_print_xml()，受 nccl 环境变量控制，而 print_xml() 则使用原始的 printf()

        重点需要注意变参数函数的处理方式。

    2. fprint_xml 同理，改成 nccl_sio_fprint_xml()，而 fprintf_xml() 使用原始的 fprintf()

    3. 给每个函数都写一个单元测试

    4. 函数名称前加 siccl_xxx()

* [v] 处理 1 条 log

* [v] 添加 graph 文件

* [v] 处理 1 条 log

* [v] 在 nccl_229 中重新编译 topo system，解除对 graph/ 的依赖

* [v] 处理 1 条 log

* [v] grpah.h / cc 对 graph/topo.h 有依赖，这个是怎么解决的？

    feedback:

    * 主要依赖 PATH_LOC ... PATH_DIS，nccl_compact.h

* [v] 处理 1 条 log

* [v] 考查 local res 的功能

    feedback:

    * get_host_hash 应该放到 utils 里

    * local_res.cc 里有 SICCL_COMPATIBLE_WITH_NCCL，这些代码都可以删掉了

    * 后续可以加入网卡检测, switch, link 等资源的检测

* [v] 处理 1 个 log

* [v] scipy 1d 卷积
