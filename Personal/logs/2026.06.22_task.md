* [v] 处理 2 个 log

* [v] 拉取 nccl 2.29 代码

* [v] 处理 2 个 log

* [v] 编译 nccl 代码

* [v] 把 gpu 02 上的 nccl 229 复制到 vms0 上，用于 codex 分析

    feedback:

    * qemu 中 /share_data 无法创建新文件/新目录，必须在 server 上创建 nccl_229

    * qemu 与 server 中没有 cuda dev 环境，无法编译 nccl

* [v] 处理 2 个 log

* [v] 处理 local_res 模块

* [v] 处理 2 个 log

* [v] 从 local res 开始，写测试

* [v] 处理 2 个 log

* [ ] 跟进 get topo system 执行顺序

* [ ] 

    ```bash
    sudo usermod -L admin
    sudo passwd -l admin
    ```

    或者直接删除：

    ```bash
    sudo userdel -r admin
    ```


    ```bash
    ip neigh | grep 100.104.94.244
    ```

    ```bash
    arp -an | grep 100.104.94.244
    ```

* [ ] direnv

* [ ] 调研 set ff=unix 的作用，因为它似乎确实无法直接删除 ^M
