*     命令模式（命令行）方法

    如果你想在执行某些操作后自动完成这个过程，可以使用命令模式：

    * **`:mark a`**：将标记 `a` 设置在当前光标位置。
    * **配合跳转指令**：
    * `:` 执行命令：`exe "normal! \`[" | mark a`

* nnoremap <leader>bk :call BackupLastChangeMarks()<CR>

    如果 <cr> 改成 ctrl+v + enter，会发生什么？
