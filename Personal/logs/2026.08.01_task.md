[processed]

* [v] `mapfile -t -d $'\0' COMPREPLY < <(find . -maxdepth 1 -type f -name "*${cur}*" -print0 | sed -z 's|^\./||' | sort -z)`

* [v] `compopt -o filenames`

* [v] `_get_comp_words_by_ref`

    ```bash
    # 使用 _get_comp_words_by_ref 获取当前光标下的单词，它能正确处理转义字符
    local cur
    _get_comp_words_by_ref -n : cur
    ```

* [v] `COMP_LINE`
