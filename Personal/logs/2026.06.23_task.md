* [v] 复写 c++ 哈希表仿函数

* [v] 处理 2 个 log

* [v] 使用函数指针作为哈希函数

    feedback:

    1. 为什么 std::hash<string>()(p.name) 无法自动推导 p.name 类型？

    2. 为什么 decltype(&calc_hash) 要加 & 才行？

* [v] 处理 1 个 log

* [v] 处理 c++ 有关 hash map 的 survey

* [v] 处理 1 个 log

* [v] 构建单机的 topo system test

    feedback:

    * nl 命令

* [v] 处理 1 个 log

* [v] 跟进 get topo system 执行顺序

* [v] 处理 1 个 log

* [ ] 

    ```bash
    sudo usermod -L admin
    sudo passwd -l admin
    ```

    或者直接删除：

    ```bash
    sudo userdel -r admin
    ```


    ```bash
    ip neigh | grep 100.104.94.244
    ```

    ```bash
    arp -an | grep 100.104.94.244
    ```

* [ ] direnv

* [ ] 调研 set ff=unix 的作用，因为它似乎确实无法直接删除 ^M
