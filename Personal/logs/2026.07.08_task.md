* [v] train / val set split

* [ ] vim `mode()`, `cursor()`, `@:`

* [ ] KFold / StratifiedKFold

    交叉验证，更稳定地评估模型

* [ ] TimeSeriesSplit

    时间序列数据，保证训练集在测试集之前

* [ ] ShuffleSplit

    多次随机划分，可控制训练/测试比例

* [ ] 洗衣服

* [ ] 整理 motion cls 的数据处理过程

* [ ] `g@`操作符

    g@ 是 Vim 内置的操作符，专门用于在 Visual 模式下调用函数，且不会退出 Visual 模式（因为它的设计就是操作符，接受一个 motion 或 Visual 选区）。

    ```vim
    " 映射为操作符
    nnoremap <leader>fn :set opfunc=FindNextSharp<CR>g@
    vnoremap <leader>fn :<C-u>call FindNextSharp(visualmode())<CR>
    ```

* [ ] vim 自定义命令

    ```vim
    " 定义命令
    command! -range FindSharp <line1>,<line2>call FindNextSharp()

    " 在 Visual 模式下使用：
    :'<,'>FindSharp
    ```

* [ ] vim ctrl + r

    在命令行中按 <C-r>= 可以插入表达式结果：

    ```vim
    " 在 Visual 模式下按 :，然后：
    :normal gv<C-r>=FindTargetLine()<CR>G
    ```
