[processed]

* [v] 编译跑通现有 test case

* [v] 生成多个 node 上的 xml 文件

* [v] AI 调研 sipu kmd 问题，为什么会有 drm

* [v] 启动第 2 个 qemu，并进行加载驱动

* [v] vms0, 1 wire guard

* [v] 跨机 siccl graph bootstrap

* [v] sicclTopoGetTopoSystem 响应环境变量 NCCL_TOPO_FILE

    先做单节点，再做多节点

    以及，load system 是否需要支持多机？似乎不需要。因为 load system 只是跳过了 local res。如果在 get system 时指定环境变量，我们也可以跳过 local res 部分。修改完 host hash 后，需要对 xml str 做合并，合并完之后我们直接就有 sicclTopoGetSystemFromXml，显然全程不需要额外的 load system。只需要在 get system 里根据环境变量判断是否需要跳过 local res 就可以了。

    再跟 nccl 对照一下，看有没有细节遗漏。如果没有的话，我们直接改 topo.h/.cc 里的内容就可以。

* [v] 跨机 (多个 host hash) xml load system

* [v] 生成多个 node 上的 xml 文件

* [v] topo.cc 中 sicclTopoGetTopoSystem 使用 ncclGetEnv 拿到 NCCL_TOPO_FILE

* [v] 验证 rankfile 的作用
